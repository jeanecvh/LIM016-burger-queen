import { FilterStatusWaiterPipe } from './filter-status-waiter.pipe';

describe('FilterStatusWaiterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStatusWaiterPipe();
    expect(pipe).toBeTruthy();
  });
});
